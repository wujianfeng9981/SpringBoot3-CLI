<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rosy.main.mapper.MeetingBookingMapper">

    <select id="findConflictingBookings" resultType="com.rosy.main.domain.entity.MeetingBooking">
        SELECT * FROM meeting_booking
        WHERE room_id = #{roomId}
        AND status IN (0, 1)
        AND is_deleted = 0
        <if test="excludeBookingId != null">
            AND id != #{excludeBookingId}
        </if>
        AND (
            (start_time &lt; #{endTime} AND end_time &gt; #{startTime})
        )
    </select>

</mapper>
